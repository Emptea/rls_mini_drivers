# IP communication test
## Работа с программой
Запуск:
```bash
cd build
sudo ./ip_comm_test <test point> <channel> <file_to_read.txt> <file_to_dump.hex>
```

Пример для запуска с 1 тестовой точки и 0 канала файла test.txt из папки build:
```bash
sudo ./ip_comm_test 1 0 test.txt dump_ch0.hex
```

При перезагрузке bitstream через Vivado перед запуском необходимо запустить из папки build файл `reload_driver.sh` через `sudo`:
```bash
cd build
sudo ./reload_driver.sh
```
Также можно это проделать из VSCode через задачу `Reload driver` (сочетание клавиш для вывода списка задач: `Ctrl+Shift+B`).

При модификации кода необходимо в папке `build` вызвать команду `make`
```bash
cd build
make
```

Для завершения программы нажмите в консоли сочетание клавиш `Ctrl+C`. По завершению программы в файл формата `.hex` с заданным именем будут выгружены по 232 отсчетов из буферов DMA приемного канала (всего `RX_BUFFER_COUNT` буферов). В файле `dump_all.hex` будут выгружены полностью структуры `channel_buffer`.

Структуру `channel_buffer` и количество буферов `RX_BUFFER_COUNT` можно посмотреть в файле `./src/dma-proxy.h`.

## Протокол общения по DMA

### Обозначения

M - прореживание (20).

Offset и Size в битах

### Структура регистра выбора канала


| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |  3   | Номер канала/диаграммы     |                                         |
| 3      |  8   | Номер кванта дальности     |                                         |
| 11     |  5   | Паддинг                    |                                         |

### Структура основного сообщения

source = 0

Повторяется в одном пакете по числу обнаружений

Отправляется на каждые M принятых входных буферов, если было обнаружение

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0     |   32 | Амп. основной диаграммы    |                                         |
| 32    |   32 | Амп. соседней диаграммы    |                                         |
| 64    |   8  | Дальность                  | 0-100 - дальняя зона, 101-141 - ближняя |
| 72    |   3  | Номер осн. даиграммы       |                                         |
| 75    |   1  | Номер сос. диаграммы       | 0 - основная минус 1, 1 - плюс 1        |
| 76    |   9  | Номер частотного канала    |                                         |
| 85    |  11  | Паддинг                    |                                         |

### Структура постзаголовка основного сообщения

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   32 | Номер посылки              | номер для N/2 отсчета в алгоритме       |
| 32     |   32 | Количество формуляров      |                                         |

### Структура raw входа

source = 1

channel 

232 отсчетов для выбраннного канала с входного FIFO

Отправляется на каждый принятый входной буфер

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура raw входа с вырезкой

source = 2

141 отсчетов для выбранного канала ближней и дальней зоны

Отправляется на каждый принятый входной буфер

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода ФАП

source = 3

141 отсчетов для выбранного канала ближней и дальней зоны после коррекции

Отправляется на каждый принятый входной буфер

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода ЛОУ

source = 4

141 отсчетов для выбранной диаграммы ближней и дальней зоны после ЛОУ

Отправляется на каждый принятый входной буфер

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода режекторного фильтра

source = 5

141 отсчетов для выбранной диаграммы ближней и дальней зоны после фильтра

Отправляется на каждый принятый входной буфер

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода ДДР

source = 6

512 отсчетов для выбраннного канала и выбранной дальности, которые уходят на БПФ.

Отправляется на каждые M принятых входных буферов

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода БПФ

source = 7

512 отсчетов для выбраннного канала и выбранной дальности, которые приходят с БПФ.

Отправляется на каждые M принятых входных буферов

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   16 | Re                         |                                         |
| 16     |   16 | Im                         |                                         |

### Структура выхода отбора по максимуму

source = 8

141 отсчет с выхода устройства отбора по максимуму до выхода АПУ

Отправляется на каждые M принятых входных буферов

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   32 | Амплитуда                  |                                         |

### Структура входа алгоритма обнаружения и измерения

source = 9

141 * 5 отсчета для N/2 последовательности из алгоритма, которые используются в обнаружении и измерении

Отправляется на каждые M принятых входных буферов

Содержит 5 отсчетов подряд - самый новый (i), предыдущий (i - 1), (i - M), i - M - 1 и i - M / 2

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   32 | Амплитуда                  |                                         |

# Структура выхода рангового устройста

source = 10

141 отсчет для N/2 последовательности из алгоритма. Выход рангового устройства

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   32 | Амплитуда                  |                                         |

# Структура выхода АПУ

source = 11

141 отсчет для N/2 последовательности из алгоритма. Выход АПУ

| Offset | Size | Name                       | Description                             |
| - | - | - | - |
| 0      |   32 | H_i                        | 1 - присутствует, 0 - отсусттвует       |
